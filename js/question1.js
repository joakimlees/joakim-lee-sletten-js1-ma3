/*

Question 1
Convert the function below to an arrow function:

function getRemainder(a,b) {
return a % b;
}

*/

const getRemainder = (a, b) => a % b;

console.log(getRemainder(5, 10));

/*Alternative code for logging the result of the getRemainder function:

const result = getRemainder(5, 10);

console.log(result);

*/
